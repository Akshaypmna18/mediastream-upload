# Changesets

This package uses manual Changesets (Option A).

1. After a meaningful change: `npm run changeset`
2. Before release: `npx changeset version`
3. Publish (when ready): `npx changeset publish`

No CI publish automation in Phase 1.
